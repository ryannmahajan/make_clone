#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <dirent.h>
#include "parse.h"

int* make_graph(){

	//pulls info from structs.

	//somehow makes the graph

	//somehow checks if cyclic. Maybe by marking if_visited

	//somehow finds the root

	//does a post-order traversal interitive
	//https://www.geeksforgeeks.org/iterative-postorder-traversal-using-stack/

	//returns a int array for order for which targets need to be compiled first. 
}
